import React from "react"
import PropTypes from "prop-types"
import neo4jd3 from "neo4jd3"

const D3 = ({ selector, options }) => {
  return <div></div>
}

D3.propTypes = {
  selector: PropTypes.string.isRequired,
  options: PropTypes.string.isRequired,
}

export default D3
