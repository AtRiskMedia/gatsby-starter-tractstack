import * as React from "react"
import PropTypes from "prop-types"

const Controller = ({ panesArray }) => {
  return <aside>Nice</aside>
}

Controller.propTypes = {
  panesArray: PropTypes.array.isRequired,
}

export default Controller
